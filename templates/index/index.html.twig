{% extends 'base.html.twig' %}

{% block title %}Main Page{% endblock %}

{% block body %}

<div class="container">
    <div class="grid">
        {% for period in periods %}
            <div class="grid-item">
                <a href="period/{{ period.id }}" class="link-reset">
                    <div>{{ period.name }}</div>
                </a>
                <div class="small-text">
                    <a href="period/{{ period.id }}/edit">
                        edytuj
                    </a> |
                    <a href="period/{{ period.id }}/delete" class="text-warning">
                        usuń
                    </a>
                </div>

            </div>
        {% endfor %}
    </div>

    <div class="mt-2">
        <a class="btn btn-primary" href="period/add">Nowy plan</a>
    </div>
    <div class="mt-2">
        <a class="btn btn-primary" href="expense/add">Nowy wydatek</a>
    </div>
</div>

{% endblock %}
